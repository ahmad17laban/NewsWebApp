@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@if (SignInManager.IsSignedIn(User))
{

    <li><a class="nav-link scrollto" href="/News">News Management</a></li>
    <li><a class="nav-link scrollto" href="/Categories">Categories Managment</a></li>

    <li class="nav-link scrollto">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"> @User.Identity.Name</a>
    </li>
    <li class="nav-link scrollto">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-link scrollto">
        <a class="" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-link scrollto">
        <a class="" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
